/**
 * Login Page for LocalStyle application
 * Entry point for user authentication
 */

import { LoginForm } from '@/features/auth/components/login-form';

export default function LoginPage() {
  return <LoginForm />;
}